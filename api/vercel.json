{
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/test.js" }
  ],

  "functions": {
    "index": {
      "handler": "index.handler",
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    { "source": "/", "destination": "/index" }
  ]
}